# Quiz

## Глава 1

- Какой URL необходимо ввести в адресной строке Chrome, чтобы открыть в браузере совершенно пустую вкладку?
  - about:blank
- Кокой комбинацией клавиш открывается консоль в Chrome?
  - COMMAND + OPTION + J
- Какие два ключевых слова используется для оъбявления новых переменных?
  - let и var
- Какой символ зачастую ставится в конце предложения?
   - ;
- Каким символом обозначается операция *деление*?
   - /
- Каким образом всегда ходят скобки?
   - Скобки ходят парами
- Какую ошибку вы получите, запустив подобное предложение:
   ```javascript
     let сумма = (9 + ; 3)
   ```
   - SyntaxError
- Какой способ использования прописных букв нужен при наименовании переменных в JavaScript?
   - ВерблюжийРегистр
- Если у вас есть открывающая ______, то всегда должна быть и закрывающая ______.
   - Скобки
- Есть ли в следующем коде синтаксические ошибки:
   ```javascript
     let моёНастроение = "Вот бы научится писать код на JavaScript";
     let моёНастроение = "не терпится использовать мои новые суперспособности";
     моёНастроение;
   ```
   - Ошибка в использовании *let* во второйстрочке кода
- Что означает одиночный знак равенства?
   - = означает присваивание

## Глава 2

- Какое значение имеет символ + в математических операциях на JavaScript?
   - Сложение двух чисел
- Каким символом обозначает умножение в JavaScript?
   ```
   - *
   ```
- Какой это тип данных? 
3456
   - Число
- Какое значение имеет символ / в математических операциях на JavaScript?
   - Деление 
- К какому типу данных будет принадлежать переменная чтоЗаТипДанных после запуска следующий строки: let чтоЗаТипДанных = 5?
   - Число
- Что используется для разделения, группировки и определения порядка выполнения математических операций, а также для того, чтобы написанное легче читалось?
   - Скобки 
- С помощью какого символа можно было бы упростить следующие предложение:
мояПеременная = мояПеременная * 2
   - *=
- Какой символ используется в JavaScript для оператора modulo?
   - %
- Найдите простейщий способ записи; как называется использованный символ?
мояПеременная = мояПеременная + 1;
   - мояПеременная++;
- Найдите простейщий способ записи; как называется использованный символ?
мояПеременная = мояПеременная - 1;
   - мояПеременная--;
- Каким образом надо *изменить* следующее предложение, чтобы оно означало <<три умножить на сумму четырёх и единицы>>
3 * 4 + 1;
   - 3 * (4 + 1)
- Если бы вы писали код, который должен был бы определять чётные и нечётные числа, какую цифру вы бы использовали с оператором modulo?
   - 2
- Используя какой символ можно было бы упростить следующие предложение:
моёЗначение = моёЗначение - 8
  - моёЗначение -= 8
- Сколько уникальных чисел в ряду ответов оператора modulo вы бы получили, если бы делали целых чисел на 5?
  - Пять чисел
- Как называют группу символов, которые сперва производят действие с переменной, а затем присваивают этой же переменной новое зачение?
   - Комбинированное
- Какой из известных нам математических операторов был бы наиболее полезен при написании подобного кода?
   - Оператор modulo (%) 

## Комплексный обзор

- Как называется этот стиль: вЭтойПеременнойЕстьПрописныеБуквы?
   - верблюжийРегистр
- Какой комбинацией клафиш открывается консоль в Chrome?
   - COMMAND + OPTION + J
- Является ли следующее предложение правильным?
var 1ыйНомерId = 1;
   - Нет, название переменной не может содержать цифры
- Является ли следующее предложение правильным?
let номерIdУченика12 = 12;
   - Да
- Сообщения об ошибках - это помощь в написании кода.
   - Так, но не всегда
- Какую ошибку выдаст консоль, если в строке есть открывающая скобка, но нет закрывающей?
   - SyntaxError.
- Каким оператором стоит воспользоваться, чтобы узнать, делится ли некое число нацело на 6?
   - % (modulo)
- Что необходимо ввести в адресной строке браузера, чтобы открыть пустую вкладку?
   - about:blank
- Каким символом можно воспользоваться, чтобы следующее предложение не изменило смысл, но было записано более коротко?
   ```
   матемЗначение = 7 + матемЗначение;
   ```
   - +=
- Является ли следующий код правильным?
var любимыеХлопья = 'Kix'; любимыеХлопья = 'Froot Loops';
любимыеХлопья;
   - Да
- Является ли следующий код правильным?
let любимыеХлопья = "Cap'n Crunch";
let любимыеХлопья = "Lucky Charms";
любимыеХлопья;
   - Нет

## Глава 3

- Комментарий к коду будет читать _______:
а) интерпретатор JavaScript;
б) человек;
в) консоль.
   - б) человек;
- Какой тип данных является наиболее часто применяемым в работе в JavaScript?
   - Строка
- Интерператор JavaScript ______ все встреченные им комментарии:
а) исполнит (запистит);
б) скомплирует;
в) проигнорирует.
   - в) прогнорирует
- Каким символом обозначается однострочный комментарий?
   - //
- Какой комбинацией клавиш делается перенос строки в консоли?
   - SHIFT + ENTER
- Строка в JavaScript заключается в ____.
   - Кавычки
- Каким символом открывается блок комментариев?
   - /*
- Каким символом закрывается блок комментариев?
   - */
- Истина/Ложь: при работе со строками намного лучше применять двойные кавычки, нежели одинарные.
   - Ложь
- Строка состоит из одного или более ______.
   - Символов
- Что означает знак + при работе со строками в JavaScript?
   - Объединение
- Какой символ нужно *добавить* в следующее предложение, чтобы избежать синтаксической ошибки?
   let цитатаИзФильма = 'The name's Bond. Kames Bond';
   - необходимо добавить \ перед апострофом
- Как бы можно было *измерить* 2 знака в следующем предложении, чтобы избежать синтаксической ошибки?
   let ущёОднаЦитатаИзФильма = 'Here's looking at you, kid.';
   - Изменить одинарные кавычки на двойные 

## Глава 4

- Как называется отдельный блок кода, который можно вызвать для выполнения какого-либо действия? 
   - Функция
- В какие символы заключается функция? 
   - {}
- Создав функцию, я могу использовать её в будущем. Как называется запуск функции по команде? 
   - Да
- В каком стиле должно быть прописано название функции? 
   - верблюжийРегистр
- Как называется часть функции, которая что-то выдаёт нам назад, после вызова функции?
   - return
- Когда мы объявляем функцию, в её скобках может быть заключена переменная (или переменные), которые называются _____. Когда мы вызываем функцию, то тому, что заключено в скобки, мы можем присвоить значения, называемые ____.
   - Параментры
- Назовите английскую аббревиатуру, обозначающую принцип, согласно которому разработчик не должен повторять один и тот же блок кода по многу раз.
   - DRY
- Какая из четырёх рассмотренных в главе встроенных функций наиболее раздражающе действует на пользователей (особенно если встречается им не один раз)?
   - alert()
- Когда предполагается задать аргументам функции более одного значения, каким символом они должны отделяться друг от друга?
   - ,
- Какая встроенная функция позволяет очень легко и просто оставлять суперсекретные сообщения для разработчиков (или для себя самого, если вы работаете над улучшением собственного кода)?
   - consle.log
- Даже если к функции ничего нельзя прибавить или внести в неё изменения, какие символы необходимы для её запуска? 
   - Скобки
- Встроенная функция нахождения случайных чисел возвращает значение больше ____ и меньше _____.
   - 0, 1: Встроенная функция нахождения случайных чисел возвращает значение больше 0 и меньше 1.
- Какая встроенная функция окрутляет число в менышую сторону до ближай- шего целого? 
   - Math.floor()
- Истина/Ложь: функция может быть «упакована» в друтую функцию. 
   - Исина
- Истина/Ложь: когда происходит вычисление значения вложенной Функции, интерпретатор сперва вычисляет значение внутренней, а затем внешней функции. 
   - Истина
- Истина/Ложь: во время работы использовать параметр внутренней функции и передаст его в качестве аргумента внешней. 
   - Ложь
- При помощи какой клавиши можно выделить код отступом, чтобы он легче читался (например, внутри функции)?
   - TAB
- Как задать переход на новую строчку в консоли?
   - SHIFT + ENTER

## Глава 5

- Как называется примитивный тип данных, имеющий строго два значения - либо true, либо false?
   - Логический тип
- Каким символом обозначается оператор сравнения, проверяющий, меньше ли значение слева значения справа?
   - <
- Истина/Ложь:"Тут есть какой-то подвох." !== 'Тут есть какой-то подвох.';
   - Ложь
- Какой оператор сравнения наиболее предпочтителен, если необходимо проверить, что два значения не равны?
   - ==
- Какие два оператора сравнения я вам рекомендовал нкогда не использовать?
   - >=
- Какой оператор сравнения проверяет, что значение справа меньше или равно значению слева?
   - <=
- Какой тип предложений используется для запуска блоков кода на основании данных условий?
   - Условный 
- Таким образов, можно сказать, что это значение является ____. Если же на основании такого условия блок кода запускаться не будет, то можно сказать, что это значение является ____.
   - Правка, ложка
- Какая часть условного выражения запускает код, если значение при блоке оператора if - false?
   - else 
- Истина/Ложь: операторы сравнения всегда выдают результаты в виде true или же false.
   - Истина
- Истина/Ложь: операторы сравнения часто используются внутри скобок пр блоке оператора if.
   - Истина
- Истина/Ложь: операторы сравнения порой могут быть использованы и в скобках в блоке else.
   - Ложь
- Истина/Ложь: конструкция if...else может быть применена лишь за пределами функции.
   - Ложь 

## Глава 6

- Как называется примитивный тип данных, означающий, что ещё никакого значения присвоено не было?
   - Underfined
- Как и операторы сравнения, ____ операторы отлично подходят для работы с услоными конструкциями.
   - Логические
- Как называется примитивный тип данных, которы ~ничего~ не означает?
   - null
- Если в объявлении функции указано четыре параментра, но при вызове было предложено лишь два чначения аргументов, какие значения получат оставшиеся два параметра?
   - Underfined
- Какой логический оператор вернёт в ответе "ложку", только если ~оба~ значения также являются "ложками"; как обозначается этот оператор?
   - ||
- Назовите все возможные значения для типа данных null.
   - null
- Истина/Ложь: 53 >= 53 && !(51 <=52)
   - Ложь
- Если условие при блоке if вернуло ложь, какую синтаксическую конструкцию можно применить, чтобы добавить ещё один вариант условия, до того как выполнить код блока else?
   - else if
- Назовите *единственный* логический оператор, который всегда возвращает булево значение; как обозначается этот оператор?
   - Логическое НЕ; !
- Истина/Ложь: так или иначе, любое значение в JavaScript можно считать либо "правкой", либо "ложкой"?
   - Истина
- Какой логический операторвернёт в ответе "правку", если хоть бы одно из значений будет "правкой"; как обозначается этот оператор?
   - Логический ИЛИ; ||
- Истина/Ложь: true || false;
   - Истина
- Истина/Ложь: false || (true && false);
   - Ложь
- Истина/Ложь: значение и null, и undefined - "ложка".
   - Истина
- Какой логический оператор вернёт в ответе "ложку", если *хотя бы одно* из значений будет "ложкой"; как обозначается этот оператор?
   - Логическое И; &&
- Истина/Ложь: null || false !== undefined;
   - Истина
- Назовите значение переменной моёСообщение:
  let моёСообщение;
   - undefined 
- Сколько существует возможных значений для типа данных undefined?
   - только одно, undefined
- Истина/Ложь: (null || false) || ((0 || true) || undefined);
   - Истина
- назовите значение переменной я:
   var я = !'уверен в себе! всё понимаю' || !!'растерян. ничего тут не понимаю';
   - true
- Какой логический оператор вернёт в ответе "правку", только если *оба* значения также являются "правками"; как обозначается этот оператор?
   - Логическое И; &&
- null, false, ' ', 0 и undefined являются _____; 'строка' и 1 - _____.
   - "ложками", "правками"
- Назовите значение переменной большаяИзНих;
  var большаяИзНих= 'вера' && 'надежда' && 'любовь';
   - 'любовь'

## Глава 7

- Истина/Ложю: объект в JS - это набор свойств.
   - Истина
- При помощи какого метода можно изменить все буквы в строке на приписные?
   - toUpperCase()
- При помощи какого клчевого слова вы можете создатьпеременную, которая никогда не будет изменяться?
   - const
- Назовите термин, которым обозначают параллельные пояснения и комментарии по использованию и действию кода.
   - Документирование
- Когда стоит использовать в коде ключевое слово let?
   - Когда значение переменной будет меняться
- Какое встроенные функции будут ожидать то пользователя нажатия на кнопку, прежде чем позволят интерпритатору продолжить работу с остальной частью кода?
   - confirm(), promt(), alert()
- Назовите термин